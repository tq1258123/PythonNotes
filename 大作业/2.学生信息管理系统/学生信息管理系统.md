# 学生信息管理系统

---

1. 满足的功能

   1. 新建学生信息
   2. 显示全部信息
   3. 查询学生信息
   4. 删除学生信息
   5. 修改学生信息
   6. 退出学生信息系统

2. 代码

   ```python
   information_list = []
   
   while 1:
       print('''
   	******************************
       欢迎使用【学生信息管理系统】
       选择您想要进行的操作
       1. 新建学生信息
       2. 显示全部信息
       3. 查询学生信息
       4. 删除学生信息
       5. 修改学生信息
       0. 退出学生信息系统
       ******************************
       ''')
       action = input('请选择你想要操作的序号：')
       if action == '1':
           name = input('请输入姓名：')
           chinese = input('请输入语文成绩：')
           math = input('请输入数学成绩：')
           english = input('请输入英语成绩：')
           total = int(chinese) + (math) + (english)
           information_list.append([name,chinese, math, english, total])
           print(information_list)
       elif action == '2':
           for information in information_list:
               print(format(*information))  # 解包
       elif action == '3':
           name = input('请输入您需要查询学生的姓名：')
           for information in information_list:
               if name in information:
                   print(information)
                   break
           else:
               print('找不到该学生信息')
       elif action == '4':
   		    name = input('请输入您需要查询学生的姓名：')
               for information in information_list:
                   if name in information:
                       info = information_list.pop(information_list.index(information))
                       print('删除信息')  # 先弹出再删除
       elif action == '5':
               for information in information_list:
                   if name in information:
       elif action == '0':
           pass
    else:
           print('输入信息有误，请重新输入')
   ```
   
   

